#include "stdio.h"
#include <string.h>

/**
 * _strlen - check the code for Main.
 * @s: variable.
 *
 *Return: the lenght of the string
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
continue;
}
return (i);
}
